define([
    'application',
    'modules/layout/directive/layout'
], function(application) {

    /**
     * Controller principal da aplicação
     */
    return application.controller('appMain', function($scope) {
    });
});